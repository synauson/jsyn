# Minimum compatible jsyn-natives-* version. Bump only when jsyn adds JNI
# calls that require a newer compiled native from synauson/synauson.
# jsyn's own version (JSYN_VERSION env var or 1.0.0-SNAPSHOT) is independent.
#
# 1.1.0 carries the WebRTC transmit-gate and native-participant teardown
# fixes that WebRtcNativeParticipantE2eIT exercises, and is the first
# release cut from the GitHub-hosted runner pipeline (the old 1.0.0
# release linked against GLIBC_2.43 and fails to load on Ubuntu 24.04).
jsynNativesVersion=1.1.0
